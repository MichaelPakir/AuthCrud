import React from 'react'

const Logout = () => {
  return (
    <section>
      <nav>
        <Link to={'/'}>
          <button>Back</button>
        </Link>
      </nav>
    </section>
  )
}

export default Logout
